# Optional short screen recording script — draft only

The bounty accepts the existing [AI-output screenshot](evidence/ai-output.png); no recording is required for this packet. If a short recording is later authorized, use the actual running app and controlled fictional fixture. Spoken lines are below; actions are separate.

## Open and analyze

```text
This is Redact. I can open an image or PDF, and QVAC runs OCR and sensitivity reasoning locally to suggest details I may want to hide.
```

> Show `chat-private.png`, select Private, and prepare for review. Wait for the real model to return. Show the 12 OCR regions and six suggestions.

## Human review

```text
The suggestions are not applied automatically. I accept a name, reject a false or intentionally retained item, and can add or adjust a mark myself.
```

> Accept a proposal, reject one proposal, draw a manual mark, and show the preview and audit history. Explain that a rejected field remains visible in the output.

## Export and limit

```text
Redact exports a separate flattened copy; it does not overwrite my original. The local model helps me review faster, but I still inspect every page because OCR can miss a field.
```

> Save the PNG and open the actual downloaded output. Show a covered target and nearby visible content. Do not describe the deliberately rejected email as protected.
